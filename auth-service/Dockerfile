FROM maven:3-jdk-11-slim AS MAVEN_TOOL_CHAIN
VOLUME /tmp
COPY target/*.jar app.jar

ENV POSTGRES $POSTGRES
ENV RABBITMQ $RABBITMQ
ENV AUTHMS $AUTHMS
ENV QUERYMS $QUERYMS
ENV ORDERMS $ORDERMS
ENV SHIPMENTMS $SHIPMENTMS
ENTRYPOINT ["java","-jar","/app.jar"]